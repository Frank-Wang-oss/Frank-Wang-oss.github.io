source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# If you want to use a standalone Jekyll version (not github-pages),
# comment out github-pages above and uncomment below.
# gem "jekyll", "~> 4.3.0"

gem "wdm", "~> 0.1.0" if Gem.win_platform?

# github-pages gem already bundles most plugins.
# Only add gems that github-pages does NOT include:
group :jekyll_plugins do
  # jekyll-remote-theme, jekyll-feed, jekyll-sitemap, jekyll-paginate,
  # jekyll-gist, jemoji, jekyll-redirect-from are all included in github-pages
end

gem "webrick", "~> 1.8"
